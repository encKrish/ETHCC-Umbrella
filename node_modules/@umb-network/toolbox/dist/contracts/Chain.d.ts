import { ethers } from 'ethers';
export declare class ChainContract {
    private contract;
    constructor(web3Provider: ethers.providers.Provider, chainContractAddress: string);
    verifyProofForBlock(blockId: number, proof: string[], key: string, value: string): Promise<boolean>;
}
//# sourceMappingURL=Chain.d.ts.map